# Implementation Prompt: Praise & Worship Hero Image Positioning Adjustment

## Objective
Update `app/[locale]/praise-worship/page.tsx` hero image positioning class to `object-top` so choir members' faces are fully visible without being cropped out at the top.

## Execution Steps
1. Update `app/[locale]/praise-worship/page.tsx` class to `object-top`.
2. Run `npm run build` to verify.
3. Commit and deploy to Vercel production.
