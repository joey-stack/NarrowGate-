# Implementation Prompt: Ministries Heading & Gallery Photos Update

## Objective
1. Adjust the Ministries section heading width (`max-w-md`) in `MinistriesGridSection.tsx` so "Service and Community" wraps onto the second line.
2. Update all 7 ministry cards with authentic photos.
3. Replace Framer stock images in `MomentsGallerySection.tsx` with user-provided church community photos.

## Execution Steps
1. Update `MinistriesGridSection.tsx` (`max-w-md` on `h2` and 7 local photo cards).
2. Update `MomentsGallerySection.tsx` with local image array.
3. Run `npm run build` to verify.
4. Commit and deploy to Vercel production.
