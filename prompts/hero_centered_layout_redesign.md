# Implementation Prompt: Hero Section Centralized Redesign

## Objective
Update `HeroSection.tsx` to:
1. Center-align location badge, headline, tagline, CTAs, indicator dots, and bottom stats cards.
2. Increase headline font size by a quarter (`text-3xl sm:text-4xl lg:text-5xl`).
3. Set slide background images positioning to `object-top` so faces are fully visible.

## Execution Steps
1. Update `HeroSection.tsx`.
2. Run `npm run build` to verify.
3. Commit and deploy to Vercel production.
