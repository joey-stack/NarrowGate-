# Implementation Prompt: Update Student Trust Fund Emblem Image Asset

## Objective
Update the Student's Trust Fund emblem graphic asset (`public/images/student-trust-fund-emblem.webp`) with the newly uploaded image provided by the user.

## Execution Steps

1. **Asset Replacement**:
   - The newly uploaded image `media__1787672294993.jpg` has been converted to optimized WebP format (`public/images/student-trust-fund-emblem.webp`).

2. **Verification & Deployment**:
   - Verify compilation with `cmd.exe /c npm run build`.
   - Capture Puppeteer full-page screenshot of `/en/support-mission/education-fund` to confirm visual presentation.
   - Commit and push to `origin/main`.
