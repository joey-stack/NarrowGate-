# Implementation Prompt: Who We Are Section Top Alignment Fix

## Objective
Update `WhoWeAreSection.tsx` so that the top of the right column (`h2` title and `p` subtitle) aligns flush with the top of the left eyebrow badge (`WHO WE ARE`).

## Execution Steps
1. Move the badge element inside `lg:col-span-5` above "Our Identity".
2. Run `npm run build` to verify layout and compilation.
3. Commit and deploy to Vercel production.
