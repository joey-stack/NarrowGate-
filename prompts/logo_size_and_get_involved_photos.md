# Implementation Prompt: Logo Size Increase & Get Involved Photos Update

## Objective
1. Increase logo height in Header (`h-14 sm:h-16 lg:h-20`) and Footer (`h-14 sm:h-18`) with adjusted header container height (`h-20 sm:h-24 lg:h-28`) for prominent branding.
2. Update Get Involved page cards (`childDedication`, `families`, `loveFeast`) with authentic local photos.

## Execution Steps
1. Copy image assets to `public/images/ministries/`.
2. Update `Header.tsx`, `Footer.tsx`, and `app/[locale]/get-involved/page.tsx`.
3. Run `npm run build` to verify.
4. Commit and deploy to Vercel production.
