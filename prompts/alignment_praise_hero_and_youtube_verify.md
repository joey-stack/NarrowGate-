# Implementation Prompt: Top Alignment, Praise Hero Image & YouTube Links Verification

## Objective
1. Move the `WHO WE ARE` badge pill inside `lg:col-span-5` above "Our Identity" in `WhoWeAreSection.tsx` so the headline top aligns flush with the eyebrow badge.
2. Update the hero banner image in `app/[locale]/praise-worship/page.tsx` to `/images/ministries/worship-choir.jpg`.
3. Verify `WatchSermonsSection.tsx` links point to `https://www.youtube.com/@thenarrowgatefoursquare`.

## Execution Steps
1. Update `WhoWeAreSection.tsx`.
2. Update `app/[locale]/praise-worship/page.tsx`.
3. Verify `WatchSermonsSection.tsx`.
4. Run `npm run build` to verify.
5. Commit and deploy to Vercel production.
